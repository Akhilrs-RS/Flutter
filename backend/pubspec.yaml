name: advocate_backend
description: A lightweight server-side Dart Shelf backend for Advocate App.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors_headers: ^0.1.5
